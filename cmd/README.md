# A1 Command Services

This directory contains the runnable command binaries for Adonis Platform One.

- `a1-gateway` — public API gateway for client requests
- `a1-worker` — task execution worker
- `a1-control-plane` — scheduler and orchestration control plane
